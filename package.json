{
  "name": "retreat-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev frontend/public --d1=DB",
    "deploy": "wrangler pages deploy frontend/public",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.4.5",
    "vitest": "^2.0.0",
    "wrangler": "^3.57.0"
  }
}
